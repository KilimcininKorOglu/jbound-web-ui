# Seed records for the dns3 development target.
# www.example.local is missing here on purpose. The diff view must report it
# as absent, which is a different class from the dns2 value mismatch.

local-data: "ns1.example.local. A 10.0.0.11"
local-data: "mail.example.local. MX 10 mx1.example.local."
