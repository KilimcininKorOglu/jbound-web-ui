# Seed records for the dns2 development target.
# www.example.local carries a different address than dns1 and dns3 on purpose.
# The diff view must report this as a value mismatch, not as a missing record.

local-data: "ns1.example.local. A 10.0.0.11"
local-data: "www.example.local. A 10.0.0.99"
local-data: "mail.example.local. MX 10 mx1.example.local."
